# FinGuard Data Directory

This directory organizes datasets across pipeline stages:

- `raw/`: Immutable raw input files (read-only; never modified)
- `processed/`: Validated, cleaned, and normalized datasets (Milestone 2)
- `quarantine/`: Records flagged with anomaly reasons during cleaning for manual review (Milestone 2)
